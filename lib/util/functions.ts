/**
 * @internal
 * Custom error messages function
 * @param msg the Error message
 * @returns
 */
export function ErrorMessage(msg: string) {
  return (
    msg +
    `\nFor more support join our discord server: ${"https://discord.com/invite/N79DZsm3m2"}`
  );
}
